import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class SafeZone here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class SafeZone extends Actor
{
    public SafeZone()
    {
        GreenfootImage safe = new GreenfootImage("safe_zone.png"); // size of box
        safe.scale(100, 100);
        setImage(safe);
    }
    
    
}
